name: test
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 probe.py
        env:
          PROBE_SLUGS: ${{ secrets.PROBE_SLUGS }}
